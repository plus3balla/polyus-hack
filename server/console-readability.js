export function logConnected(who) { console.log(`${who} connnected`); }
export function logDisconnected(who) { console.log(`${who} disconnnected`); }
export function logError(message) { console.log(message); }
;
