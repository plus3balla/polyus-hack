{
  "name": "echo-server",
  "version": "0.0.1",
  "description": "A server that echoes message it receives to all connected websockets except the one that sent it",
  "main": "echo-server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "server",
    "js",
    "node"
  ],
  "author": "cocacola-lover",
  "license": "ISC",
  "dependencies": {
    "ws": "^8.9.0"
  },
  "devDependencies": {
    "@types/node": "^18.8.5"
  }
}
