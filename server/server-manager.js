var WebSocketModule = require('ws');
var fs = require('fs');
var images = [];
var convertImagesToBase64 = function (dir) {
    images.push();
};
var server = new WebSocketModule.Server({
    port: 8080
});
