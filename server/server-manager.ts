const WebSocketModule = require('ws');
const fs = require('fs');

const images:string[] = [];

const convertImagesToBase64 = (dir:string) => {
    images.push()
}

const server = new WebSocketModule.Server({
    port: 8080
});




